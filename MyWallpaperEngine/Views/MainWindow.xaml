<Window x:Class="MyWallpaperEngine.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:vm="clr-namespace:MyWallpaperEngine.ViewModels"
        mc:Ignorable="d"
        Title="MyWallpaperEngine - Pro Build" Height="650" Width="1000"
        WindowStartupLocation="CenterScreen"
        
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <!-- Liga a Tela ao Cérebro -->
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Título e Status -->
        <TextBlock Grid.Row="0" Text="{Binding StatusMessage}" FontSize="20" FontWeight="Bold" Margin="0,0,0,15" Foreground="{DynamicResource PrimaryHueMidBrush}"/>

        <!-- A Galeria de Fotos -->
        <ListBox Grid.Row="1" ItemsSource="{Binding Wallpapers}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" BorderThickness="0" Background="Transparent">

            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <materialDesign:Card Width="220" Margin="10" UniformCornerRadius="8" materialDesign:ElevationAssist.Elevation="Dp3">
                        <StackPanel>
                            <!-- A Miniatura -->
                            <Border Height="130" ClipToBounds="True">
                                <Image Source="{Binding CaminhoCompleto}" Stretch="UniformToFill" />
                            </Border>

                            <!-- O Nome da Foto -->
                            <TextBlock Text="{Binding NomeExibicao}" FontWeight="SemiBold" Margin="10,12,10,5" TextTrimming="CharacterEllipsis" ToolTip="{Binding CaminhoCompleto}"/>

                            <!-- Botão estilo Preenchido (Raised) e Adicionei um ToolTip -->
                            <Button Content="Definir Fundo" 
                                    Command="{Binding DataContext.AplicarWallpaperCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" 
                                    CommandParameter="{Binding}" 
                                    Margin="10,0,10,12" 
                                    Style="{StaticResource MaterialDesignRaisedButton}"
                                    ToolTip="Aplicar este papel de parede agora"
                                    Cursor="Hand"/>
                        </StackPanel>
                    </materialDesign:Card>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Rodapé com os Controles -->
        <Grid Grid.Row="2" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Botão Importar -->
            <Button Grid.Column="0" Content="📂 Importar Pasta" 
                    Command="{Binding SelecionarPastaCommand}" 
                    Style="{StaticResource MaterialDesignRaisedButton}" 
                    Background="#28A745" BorderBrush="#28A745" Foreground="White" Height="40" Padding="15,0"
                    Cursor="Hand"/>

            <!-- Seletor de Tempo -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,20,0">
                <TextBlock Text="Trocar a cada:" VerticalAlignment="Center" Margin="0,0,8,0" FontWeight="SemiBold"/>
                <ComboBox ItemsSource="{Binding OpcoesTempo}" SelectedItem="{Binding TempoSelecionado}" Width="60" Margin="0,0,8,0"/>
                <TextBlock Text="min" VerticalAlignment="Center" FontWeight="SemiBold"/>
            </StackPanel>

            <!-- Botão do Timer / Shuffle -->
            <Button Grid.Column="3" Content="{Binding TextoBotaoTimer}" 
                    Command="{Binding ToggleTimerCommand}" 
                    Style="{StaticResource MaterialDesignRaisedButton}" 
                    Background="{DynamicResource PrimaryHueMidBrush}" BorderBrush="{DynamicResource PrimaryHueMidBrush}" 
                    Foreground="White" Width="220" Height="40"
                    Cursor="Hand"/>
        </Grid>
    </Grid>
</Window>